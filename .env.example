# Bearer token Vercel Cron presents to /api/refresh.
# Leave unset locally: the route stays open when no secret is configured.
CRON_SECRET=

# Absolute origin used for canonical URLs and share-card images.
# Unset, production falls back to the deployed URL and previews to their own.
NEXT_PUBLIC_SITE_URL=
