# pnpm >=12 reads settings from here, not from package.json.
# unrs-resolver is the native resolver behind eslint-config-next; it needs
# its postinstall to build. Declared so CI/Vercel installs never prompt.
allowBuilds:
  unrs-resolver: true
